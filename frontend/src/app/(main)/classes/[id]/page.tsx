export default function Page ({ props: { id } }: { props: { id: string } }) {
  // id es el id de la clase
  return (
    <p>Muestra mas detalles de la clase reservada que tienes ya ses alumno o maestro</p>
  )
}
